# --- LDR Coach frontend configuration ---
# Base URL the browser uses to reach the API. With the bundled Nginx
# reverse proxy this is a same-origin relative path; set an absolute URL
# only when the API lives on a different host.
NEXT_PUBLIC_API_URL=/api
